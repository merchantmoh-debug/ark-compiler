// The "Glenn Lewis" Verification Test
// Objective: Prove that the compiler screams if a linear resource (MEMORY BUFFER) is dropped.

func main(args) {
    print("Allocating linear memory...")
    
    // sys.mem.alloc returns a Buffer, which is marked linear in checker.rs
    buf := sys.mem.alloc(1024)
    
    // CRITICAL: We allocate 'buf' but we DO NOT consume it or return it.
    // This MUST be a compile error: "Linear variable 'buf' dropped without consumption."
    
    // To fix this test, we would need to pass 'buf' into a consuming function like:
    // sys.mem.inspect(buf)
    
    print("Function ending, 'buf' will be dropped implicitly.")
}
